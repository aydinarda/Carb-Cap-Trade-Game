# dependencies
node_modules/

# build output
dist/
Carbon_Cap_and_trade___Game/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# env
.env
.env.*
!.env.example

# os
.DS_Store

# simulation output (single sqlite file, regenerated)
sim/out/

# throwaway sweep scripts (analysis, not part of the app)
sim/sweeps/

# per-round score/trade logs from a live or load-test run (SCORE_LOG / TRADE_LOG)
game-logs/

# local design notes and hand-offs (not part of the app)
notes/

# RL agent training — local only. Anchored to the root: the trained agents the host can add
# live in server/rl/ and must be committed.
/rl/
